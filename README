1. INSTALLATION
----------------
This tool is compatible with Python 2.7+ and Python 3

To install dependencies:
1. Optionally activate your virtualenv
2. run 'pip install -r requirements.txt' to install python packages


2. EXECUTION
-------------
The application accepts input on STDIN and prints to STDOUT. Console
input/output redirection may be used, e.g.
$ python <fixtures.txt >output.txt

3. TESTING
-----------
To execute tests, user the 'nosetests' command while in the project folder

